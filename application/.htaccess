<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Jika file / folder tidak ada → lanjutkan ke index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
